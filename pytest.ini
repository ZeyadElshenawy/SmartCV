[pytest]
DJANGO_SETTINGS_MODULE = smartcv.settings
python_files = test_*.py
testpaths = tests
markers =
    integration: end-to-end pipeline tests; require profile + JD fixtures (slow when recording, fast on replay)
    integration_recorded: integration tests that use recorded LLM responses (fast, CI-safe subset)
